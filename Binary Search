class Solution {
    int i=0;
    public int search(int[] nums, int target) {
        if(i==nums.length){
            return -1;
        }
        if(nums[i]==target){
            return i;
        }
        i++;
        return search(nums,target);
    }
}
